# world 开发态提示

适用于：

- `tenant/world 隔离怎么推进`
- `继续做 world isolation`
- `检查 tenant namespace contract`

执行顺序：

1. 读取 `references/local-runtime.md`
2. 先读 `docs/HANDOFF.md` 与 `current-status.md` 中 tenant/world 方向
3. 再看 `scripts/world_cli.py` 与 `src/we_together/services/world_service.py`
4. 对 contract、负向测试、边界补强给出结论或直接实现
