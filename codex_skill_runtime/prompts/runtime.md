# we-together-runtime / 运行态

1. 先读取 `references/local-runtime.md`
2. 使用其中的 MCP server 名称
3. 优先工具：
   - `we_together_graph_summary`
   - `we_together_list_invariants`
   - `we_together_check_invariant`
   - `we_together_self_describe`
