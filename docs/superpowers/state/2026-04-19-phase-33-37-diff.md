# Phase 33-37 Diff Report — v0.13.0 → v0.14.0

**Date**: 2026-04-19
**Test baseline**: 436 → **477 passed** (+41)
**ADR**: 33 → 39 (+6)
**Migrations**: 14 → 15 (+1, `0015_media_assets`)
**Benchmarks**: 8 → 9 (+1, `multimodal_retrieval_groundtruth`)
**不变式**: 18 → 20

## 新增模块

```
src/we_together/
├── services/
│   ├── time_simulator.py         ★ NEW (Phase 34)  TickResult / TickBudget / run_tick / simulate
│   ├── tick_sanity.py            ★ NEW (Phase 34)  check_growth / check_anomalies / evaluate
│   ├── media_asset_service.py    ★ NEW (Phase 35)  register / list / link / visibility
│   └── ocr_service.py            ★ NEW (Phase 35)  ocr_to_memory / transcribe_to_event

db/migrations/
└── 0015_media_assets.sql         ★ NEW

benchmarks/
└── multimodal_retrieval_groundtruth.json  ★ NEW

scripts/
├── simulate_week.py              ★ NEW  7 天 tick CLI
├── import_image.py               ★ NEW  单图 OCR 导入
├── bench_scale.py                ★ NEW  10k+ 规模压测
├── verify_skill_package.py       ★ NEW  解包 zip smoke
└── demo_openai_assistant.py      ★ NEW  MCP → OpenAI function schema

tests/
├── runtime/test_phase_33_skill_host.py    ★ NEW  12 tests
├── services/test_phase_34_tick.py         ★ NEW  11 tests
├── services/test_phase_35_media.py        ★ NEW   8 tests
└── services/test_phase_36_debt.py         ★ NEW  10 tests

docs/superpowers/decisions/
├── 0034-phase-33-skill-schema-v1.md       ★ NEW
├── 0035-phase-33-skill-host.md            ★ NEW
├── 0036-phase-34-tick-loop.md             ★ NEW
├── 0037-phase-35-media-assets.md          ★ NEW
├── 0038-phase-36-scale-debt.md            ★ NEW
└── 0039-phase-33-37-synthesis.md          ★ NEW

docs/superpowers/state/
├── 2026-04-19-service-inventory.md        ★ NEW
├── 2026-04-19-migration-audit.md          ★ NEW
└── 2026-04-19-phase-33-37-diff.md         ★ NEW (本文件)

docs/superpowers/plans/
└── 2026-04-19-phase-33-37-mega-plan.md    ★ NEW
```

## 修改模块（向后兼容）

| 文件 | 修改 |
|------|------|
| `runtime/skill_runtime.py` | + `SKILL_SCHEMA_VERSION="1"` + `schema_version` 字段 + `from_dict` |
| `runtime/adapters/mcp_adapter.py` | + `build_mcp_resources` + `build_mcp_prompts`；tools 2 → 6 |
| `scripts/mcp_server.py` | + resources/list + resources/read + prompts/list + prompts/get |
| `services/vector_index.py` | + `SUPPORTED_BACKENDS` + `_require_sqlite_vec` + `_require_faiss` |
| `tests/runtime/test_prompt_composer.py` | 兼容 schema_version 字段 |

## ADR 新增

| # | 标题 | 支柱 |
|---|------|------|
| 0034 | Phase 33 SkillRuntime v1 Schema 冻结 | B |
| 0035 | Phase 33 真 Skill 宿主（MCP/OpenAI/Claude） | B |
| 0036 | Phase 34 持续演化 Tick 闭环 | C |
| 0037 | Phase 35 媒体资产落盘 | C |
| 0038 | Phase 36 规模化 & 债务清理 | A |
| 0039 | Phase 33-37 综合 + 不变式 18 → 20 条 | 全局 |

## 不变式增量

**#19**：SkillRuntime 请求/响应 schema 必须版本化；破坏性变更需 v2，而不是 in-place 改字段。
**#20**：tick 写入必须能在无人工干预下被 snapshot 回滚至任一时间点（闭环可逆）。

## 接口稳定性

- `we_together.runtime.SkillRuntime` 接口**正式冻结 v1**（ADR 0034）
- 现有 adapter（Claude / OpenAI / MCP / Feishu / LangChain / Coze）自动接受带 `schema_version` 的请求
- 旧 runtime 代码创建 SkillRequest 不填 `schema_version` 时默认 "1"，兼容

## 三支柱达成度

| 支柱 | v0.13 | v0.14 | 提升点 |
|------|:-----:|:-----:|--------|
| A 严格工程化 | 9/10 | **9.5/10** | service-inventory + migration-audit + #19/#20 不变式 |
| B 通用型 Skill | 6/10 | **8/10** | schema v1 冻结 + MCP 全协议 + OpenAI demo + verify_skill |
| C 赛博生态圈 | 5/10 | **7/10** | tick 闭环 + 媒体落盘 + 合理性评估 |

## 留给 v0.15 的事

- 真 sqlite-vec / FAISS 集成 + 真 10k-100k 压测报告归档
- multi_agent_chat.py REPL + simulate_week 真执行报告
- federation 真 RPC 协议实现（替换 stub）
- tick 真调度示例（crontab / NATS-trigger）
- Claude Skills marketplace 上架流程
- contradiction → patch 自动联动（需先放宽 #18）
- i18n prompts

## 验收

```bash
.venv/bin/python -m pytest -q
# 477 passed in ~13s

git log --oneline v0.13.0..HEAD | wc -l
# 5+ 次小步提交（Phase 33 / 34 / 35 / 36 / 37）
```
