# Phase 28-32 Diff Report — v0.12.0 → v0.13.0

**Date**: 2026-04-19
**Test baseline**: 410 → **436 passed** (+26)
**ADR**: 27 → 33 (+6)
**Migrations**: 13 → 14 (+1, `0014_proactive_prefs`)
**Benchmarks**: 7 → 8 (+1, `contradiction_groundtruth`)

## 新增模块

```
src/we_together/
├── agents/                      ★ NEW (Phase 29)
│   ├── __init__.py
│   ├── person_agent.py          # PersonAgent.from_db / speak / decide_speak
│   └── turn_taking.py           # next_speaker / orchestrate_multi_agent_turn
├── db/
│   └── backends.py              ★ NEW (Phase 28)  SQLiteBackend + PGBackend
├── eval/
│   └── contradiction_eval.py    ★ NEW (Phase 31)  P/R 评估
├── llm/providers/
│   └── multimodal_embedding.py  ★ NEW (Phase 32)  Mock + CLIPStub + cross_modal
└── services/
    ├── contradiction_detector.py ★ NEW (Phase 31)
    ├── embedding_cache.py        ★ NEW (Phase 28)  EmbeddingLRUCache
    ├── proactive_agent.py        ★ NEW (Phase 30)  Trigger + Intent + scan
    ├── proactive_prefs.py        ★ NEW (Phase 30)  mute/consent/is_allowed
    └── vector_index.py           ★ NEW (Phase 28)  flat_python + filter

db/migrations/
└── 0014_proactive_prefs.sql     ★ NEW

benchmarks/
└── contradiction_groundtruth.json  ★ NEW

tests/services/
├── test_phase_28_scale.py        ★ NEW   9 tests
└── test_phase_29_30_31_32.py     ★ NEW  17 tests
```

## 修改模块（向后兼容）

| 文件 | 修改 |
|------|------|
| `runtime/sqlite_retrieval.py` | + `query_text` / `embedding_client` 参数（默认 None 走原路径） |
| `services/embedding_recall.py` | + `filter_person_ids` 参数 |
| `services/memory_cluster_service.py` | + `use_embedding=True/False` Jaccard fallback |
| `services/event_bus_service.py` | + `NATSBackend.drain` 真实现（subscribe + timeout） |

## ADR 新增

| # | 标题 |
|---|------|
| 0028 | Phase 28 Vector Index & Scale |
| 0029 | Phase 29 Multi-Agent Society |
| 0030 | Phase 30 Proactive Graph |
| 0031 | Phase 31 Meta-cognition |
| 0032 | Phase 32 Multimodal Native |
| 0033 | Phase 28-32 综合 + 不变式 16 → 18 条 |

## 不变式增量

**#17**：多 agent 必须共享底层图谱真理；private vs shared 是查询过滤，不是物理拷贝。
**#18**：主动写入（L1/L2）必须经预算 + 偏好门控；detection-only 服务只读不写。

## 接口稳定性

- `we_together.runtime.SkillRuntime` 接口零破坏
- `chat_service.run_turn` 接口零破坏
- `patch_applier.apply_patch_record` 操作集合不变
- 所有新参数默认值 → 旧调用保持原路径

## 留给 v0.14 的事

- 真 sqlite-vec / FAISS 集成 + 1M 规模压测报告
- multi_agent_chat.py REPL + simulate_week.py CLI
- 真 cron / NATS-trigger 调度 proactive_scan
- contradiction → patch 自动联动（高置信 → create_local_branch）
- multimodal 真接入：media_assets migration + Skill 渲染图片消息
- federation interop 真 RPC（替换 stub）
- PyPI testpypi（需 token）

## 验收

```bash
.venv/bin/python -m pytest -q
# 436 passed in ~12s
```

```bash
git log --oneline v0.12.0..HEAD | wc -l
# 多次小步提交（VI / MA / PG / MC / MN + EPIC）
```
