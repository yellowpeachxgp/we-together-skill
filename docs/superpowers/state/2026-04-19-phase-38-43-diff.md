# Phase 38-43 Diff Report — v0.14.0 → v0.15.0

**Date**: 2026-04-19
**Test baseline**: 477 → **521 passed** (+44)
**ADR**: 39 → 45 (+6)
**Migrations**: 15 → 16 (+1, `0016_activation_traces`)
**Benchmarks**: 9 → 10 (+1, 首份 tick_run baseline)
**不变式**: 20 → **22**

## 新增模块

```
src/we_together/services/
├── tick_cost_tracker.py          ★ NEW (Phase 39)
├── activation_trace_service.py   ★ NEW (Phase 40)
├── forgetting_service.py         ★ NEW (Phase 41)
├── entity_unmerge_service.py     ★ NEW (Phase 41)
└── federation_client.py          ★ NEW (Phase 42)

db/migrations/
└── 0016_activation_traces.sql    ★ NEW

scripts/
├── dashboard.py                  ★ NEW (Phase 38)
├── skill_host_smoke.py           ★ NEW (Phase 38)
├── rollback_tick.py              ★ NEW (Phase 39)
├── activation_path.py            ★ NEW (Phase 40)
└── federation_http_server.py     ★ NEW (Phase 42)

docs/
├── getting-started.md            ★ NEW (Phase 38)
├── tick-scheduling.md            ★ NEW (Phase 39)
├── hosts/claude-desktop.md       ★ NEW (Phase 38)
├── hosts/claude-code.md          ★ NEW (Phase 38)
├── hosts/openai-assistants.md    ★ NEW (Phase 38)
├── superpowers/specs/federation-protocol-v1.md  ★ NEW (Phase 42)
└── superpowers/decisions/
    ├── 0040-phase-38-consumption-ready.md
    ├── 0041-phase-39-tick-runs.md
    ├── 0042-phase-40-neural-mesh.md
    ├── 0043-phase-41-forgetting-unmerge.md
    ├── 0044-phase-42-federation-mvp.md
    └── 0045-phase-38-43-synthesis.md

benchmarks/tick_runs/
└── 2026-04-18T19-37-40Z.json     ★ NEW 首份 baseline

tests/
├── runtime/test_phase_38_cr.py   ★ NEW (+8)
├── services/test_phase_39_ct.py  ★ NEW (+10)
├── services/test_phase_40_nm.py  ★ NEW (+10)
├── services/test_phase_41_fo.py  ★ NEW (+10)
└── services/test_phase_42_fd.py  ★ NEW (+6)
```

## 修改模块（向后兼容）

| 文件 | 修改 |
|------|------|
| `services/time_simulator.py` | Bug fix: `_make_snapshot_after_tick` SQL 改用真实 snapshots 列 |
| `scripts/simulate_week.py` | 拆 `build_report` + `archive`；`--archive` 开关 |

## ADR 新增

| # | 标题 | 支柱 |
|---|------|:---:|
| 0040 | Phase 38 消费就绪 | B |
| 0041 | Phase 39 Tick 真运行 + 归档 | C |
| 0042 | Phase 40 神经网格式激活 | C |
| 0043 | Phase 41 遗忘/压缩/拆分 | A + C |
| 0044 | Phase 42 联邦 MVP Read-Only | B |
| 0045 | Phase 38-43 综合 + 不变式 20→22 | 全局 |

## 不变式增量

**#21** 任何激活传播机制必须可 introspect（能画出"谁激活了谁、权重多少"）。

**#22** 图谱写入必须有对称的"撤销"路径（merge ↔ unmerge / archive ↔ reactivate / create ↔ mark_inactive）。

## 接口稳定性

- `SkillRuntime` v1 schema 继续冻结（不变式 #19）
- `services/federation_client` 新接口不依赖既有方法变更
- `services/forgetting_service` / `entity_unmerge_service` / `activation_trace_service` 全是新增
- `time_simulator` bug fix 让之前静默失败的 snapshot 真写入（可能导致某些环境下的 snapshots 表体积增长）

## 三支柱达成度

| 支柱 | v0.14 | v0.15 | 改进点 |
|------|:-----:|:-----:|--------|
| A 严格工程化 | 9.5 | **9.5** | 新增 2 条不变式 + service inventory 更新 |
| B 通用型 Skill | 8 | **9.5** | 三路宿主文档 + Dashboard + 联邦 MVP |
| C 赛博生态圈 | 7 | **8.5** | tick 归档 + 神经网格 + 遗忘闭环 |

## 留给 v0.16 的事

- 真 LLM 跑 tick + 成本真实报告
- 真 sqlite-vec / FAISS 集成
- 联邦写路径 + 鉴权
- Claude Skills marketplace 上架
- multi_agent_chat.py REPL + simulate_week 真执行
- contradiction → unmerge → patch 带人工 gate 的 workflow
- plugin / extension 机制
- i18n prompts
- tick 自动触发 archive_stale_memories

## 验收

```bash
.venv/bin/python -m pytest -q
# 521 passed

git log --oneline v0.14.0..HEAD | wc -l
# 6 个 commit（Phase 38/39/40/41/42/43-EPIC）
```
