# Phase 25-27 Graph Summary Diff Report（v0.12.0 基线）

时间：2026-04-19

## 基线对比

| 维度 | v0.11.0 | v0.12.0 |
|---|---|---|
| 测试 | 392 passed | **410 passed**（+18） |
| migrations | 0001-0012 | 0001-**0013**（+1） |
| ADR | 0001-0023 | 0001-**0027**（+4） |
| Coverage | 未测 | **90%** |
| LLM Protocol 方法 | 2（chat / chat_json） | **4**（+ chat_with_tools / chat_stream） |
| Embedding provider | 无 | **3**（Mock / OpenAI / sentence-transformers） |
| WAL 模式 | 默认 rollback | **WAL** 启用 |
| CLI optional extras | 4 | **7**（+ embedding / nats / redis） |
| GitHub workflow | ci / docker | + **publish** (tag-push auto release) |

## 新增能力面

1. **真 LLM 集成**：agent_runner 走原生 tool_use；chat_stream 支持 Anthropic / OpenAI / Mock
2. **向量化**：embedding provider + migration 0013 + BLOB 编解码 + cosine top_k
3. **双路径 mock**：真 SDK 延迟 import，测试全走 Mock，CI 零网络
4. **生产化**：v0.12.0 wheel 隔离验证 / publish-on-tag workflow / coverage 基线 90%

## Mock embedding benchmark 示例

```
benchmark: embedding_retrieval_v1
- query "工作会议 项目 代码" top-5 → 应命中 [m_work_1, m_work_2]
- query "周末 娱乐 休息" top-5 → 应命中 [m_life_1, m_life_2]
```

## 结论

v0.12.0 三个关键跃迁：

1. **Agent loop 真通 Claude/OpenAI**：从 "mock 协议 → 原生 tool_use"
2. **图谱加维度：语义**：embedding 让 memory 匹配脱离字面
3. **工程化闭环**：tag → Actions → PyPI 的自动化路径铺好
