# Phase 22-24 Graph Summary Diff Report（v0.11.0 基线）

时间：2026-04-19

## 基线对比

| 维度 | v0.10.0 | v0.11.0 |
|---|---|---|
| 测试 | 349 passed | **392 passed**（+43） |
| migrations | 0001-0010 | 0001-**0012**（+2） |
| ADR | 0001-0019 | 0001-**0023**（+4） |
| Specs / RFC | - | +联邦协议 RFC |
| importers | 15 | **18**（+ CSV migration / Notion export / Signal export） |
| adapters | 6 + MCP 真启动 | **6** + 真 tool_use 多轮 + streaming 骨架 |
| simulation 能力 | 5 | 5 + narrative_service + associative_recall |
| eval 种类 | 3 | 3 |
| 事件总线 backend | jsonl + NATS stub | **5**（+ NATS real / RedisStream real + Backend Protocol 统一） |
| 真集成测试 | 0 | **6**（bootstrap→...→eval 真跑） |
| CLI 数量 | 26 | **30+**（+ migrate / graph-io / narrate / analyze） |

## 新增能力面（v0.11.0 首创）

1. **跨 skill 实例 person 引用真生效**（联邦）
2. **图谱可在不同 we-together 间迁移**（canonical JSON round-trip）
3. **Agent tool_use 真多轮循环**（落 events 表）
4. **图谱章节化 / 叙事聚合**（narrative_arcs）
5. **同一 event 多视角记忆**（perceived_memory）
6. **图谱级分析**（度/密度/孤立）
7. **图谱入 PyPI 生态**（wheel 隔离验证通过）
8. **CI + pre-commit 基础设施**

## 新增 Benchmarks

与 v0.10.0 持平（6 个）。Phase 22-24 未新增 benchmark，重点在基础设施与能力面。

## 结论

v0.11.0 三个关键跃迁：

1. **从单机到多机**：federation_fetcher + 事件总线真后端 = "赛博生态圈"从名词变动词
2. **从 mock 到真跑**：tests/integration/ + wheel 隔离验证 = 工程外部可信度首次建立
3. **从关系到叙事**：narrative_arcs + perceived_memory + analytics = 图谱首次有"故事"层

下一轮候选（Phase 25+）：
- 规模与真依赖（100 万 person / imagehash / chromaprint / PG backend）
- 真 HITL + RBAC 整合（branch_console fastapi 升级）
- i18n prompt（zh / en / ja）
- 真 streaming（Claude/OpenAI SDK 接入）
- PyPI 正式发布
